# Tray

Camera trajectory estimation on the TUM RGB-D dataset using epipolar geometry
(on `freiburg1_xyz`) and ICP (on `freiburg2_pioneer_slam`).

See `INDEX.md` for the full Spanish specification and `CLAUDE.md` for commands
and code layout. Datasets are downloaded with `bash scripts/download_tum.sh`.
